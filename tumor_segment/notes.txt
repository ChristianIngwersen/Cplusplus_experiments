   //double test = std::accumulate( intensities.begin(), intensities.end(), 0.0)/intensities.size();
    // Mean
    double tumor_avg = cv::mean(intensities)[0]; 
    double other_avg = cv::mean(image)[0];
    // Compute squared difference
    std::vector<double> diff(intensities.size()); 
    std::transform(intensities.begin(), intensities.end(), diff.begin(), [tumor_avg](double x) { return x - tumor_avg; });

    // Standard deviation
    double sq_sum = std::inner_product(diff.begin(), diff.end(), diff.begin(), 0.0);
    double stdev = std::sqrt(sq_sum / (intensities.size()-1));